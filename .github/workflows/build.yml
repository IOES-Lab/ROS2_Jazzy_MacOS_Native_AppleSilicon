name: Build

on:
    push:
        branches: [ "master" ]
    pull_request:
        branches: [ "master" ]
    workflow_dispatch:

jobs:
    build:
        # runs-on: macos-latest
        runs-on: dockurr/macos
        env:
            VERSION: "ventura"
        steps:
            - uses: actions/checkout@v3
            
            - name: Run install script
              run: /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/IOES-Lab/ROS2_Jazzy_MacOS_Native_AppleSilicon/main/install.sh)"